package com.ingthor.arrays_and_strings;

/**
 * Created by Ingthor on 06/04/2017.
 */
public class OneAway_1_5 {

    //todo finish
    /*
    public static void OneAway(String str1, String str2)
    {
        boolean sameLength = str1.length() == str2.length();

        String shorter;
        if(str1.length() > str2.length())
        {
            shorter = str2;
        }
        else
        {
            shorter = str1;
        }

        boolean oneErorr =false;
        int str1Offset = 0;
        int str2Offset = 0;
        int failIndex = 0;
        boolean firstPassFailed = false;
        for(int i = 0; i < shorter.length();i+=)
        {

            if(str1.charAt(i+str1Offset) != str2.charAt(i+str2Offset))
            {
                oneErorr = true;
                //we had an edit do nothing
                if(!sameLength)
                {
                    failIndex = i;
                    if(str1.charAt(i+1) == str2.charAt(i))
                    {
                        str1Offset++;
                    }
                    if(str2.c)
                }
            }
        }


    }

*/


}
